---
name: dashboard-design
description: 制御画面（apps/mcp-server のWeb UI）のデザインを定義・適用・点検するときに使う。デザインシステムの改訂、画面の骨格や配色や操作階層の変更、既存画面が役割分解と一致しているかの確認を扱う。発表スライド側のデザインは対象外。
---

# 制御画面デザイン

このリポジトリでは `.agents/skills/dashboard-design/SKILL.md` を正本とする。正本を最初から最後まで読み、そこから参照される `references/` のファイルと `docs/dashboard-roles.md` を読み、指示に従う。
