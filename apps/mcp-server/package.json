{
  "name": "@ultimate-freestyle/mcp-server",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler dev --config wrangler.jsonc",
    "build": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy --config wrangler.jsonc --dry-run --outdir dist",
    "deploy": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy --config wrangler.jsonc",
    "smoke": "node scripts/smoke.mjs",
    "test": "WRANGLER_LOG_PATH=.wrangler/wrangler.log vitest run",
    "typecheck": "tsc --noEmit",
    "types": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler types src/worker-configuration.d.ts --config wrangler.jsonc --include-runtime=false",
    "types:check": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler types src/worker-configuration.d.ts --config wrangler.jsonc --include-runtime=false --check",
    "check": "bun run types:check && bun run typecheck && bun run test && bun run build"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "0.8.2",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@ultimate-freestyle/research-schema": "workspace:*",
    "agents": "^0.17.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.6",
    "@cloudflare/workers-types": "5.20260718.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.112.0"
  }
}
